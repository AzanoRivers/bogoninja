---
interface Props {
    text: string;
    className?: string;
}

const { text, className = "" } = Astro.props;
---

<p class={`scroll-reveal text-left text-lg sm:text-sm md:text-base lg:text-lg xl:text-xl px-2 font-light leading-tight ${className}`}>
    {text}
</p>
